# 07 — Доступность, скорость, SEO и юридические страницы

**Требования:** R42, R43, R44, R45, R45.1, R46, R50i, R51i, R52i, R58, G01, R25.1, R30.1
**Blocked by:** 06
**Зона:** `app/[locale]/impressum/` · `app/[locale]/datenschutz/` · `app/[locale]/layout.tsx` (метаданные) · `public/og/` · `public/icons/` · `tests/e2e/` · `next.config.*`
**Волна:** 5
**Status:** ready

## Что должно заработать

Сайт можно отдавать людям. Он проходится с клавиатуры и читается скринридером;
человеку, которому плохо от движения, он отдаёт тот же рассказ без движения;
человеку с фоточувствительностью вспышки не вредят. Ссылка, кинутая в мессенджер,
разворачивается в нормальную карточку на нужном языке. Lighthouse на десктопе
выше 95 по всем четырём категориям.

Есть `/де/impressum` и `/de/datenschutz` (и то же на `en`, `ru`) — со ссылками
в подвале и видимыми заглушками вместо реквизитов и юридического текста.
**Юридический текст не выдумывается**: в шаблоне стоит явная пометка, что текст
даёт юрист владельца, и перечислено, какие поля конфига в него подставляются.

## Из брифа, дословно

> «Оптимизация», «Accessibility»
> «prefers-reduced-motion», «цель: 60 FPS», «Lighthouse >95»
> дополнение: «Да, две страницы-заглушки» (Impressum и Datenschutz)
> дополнение: «Lighthouse >95 меряем на десктопе, на мобильном целимся 90+»

## Разделы спецификации

Истории 21, 22, 23, 24, 25, 26, 27, 39, 46, 47, 48; Решения §1, §11, §12, §13.

## Пришло с ревью волны 3 — чинится здесь

- [ ] **Палитра сведена в один источник.** Сейчас её четыре копии: `styles/tokens.css`, `tests/palette.test.ts`, `tests/flash.test.ts`, `tests/hero.test.ts`. WCAG-математика — три реализации (`components/FlashTransition/luminance.ts` и две в тестах). Это единственная находка, повторившаяся в каждом ревью.
- [ ] **Фикстура сцен больше не делит id с живыми секциями.** `lib/motion/SceneFixture` брала `cards`, живые How It Works / Benefits / Prices сместили её части с 0/1 на 2/3 — `e2e/motion.spec.ts` красный. Либо фикстуре собственный id, либо тест сравнивает индексы относительно.
- [ ] **Высота панели шапки заведена токеном.** `HeroSection.module.css` повторяет её числом `3.875rem` из чужой вёрстки; умеренная правка шапки разъедет hero, а e2e останется зелёным.
- [ ] **`<Prices/>` встаёт на страницу сам.** Сейчас он отрендерен внутри Benefits в обход `app/[locale]/page.tsx` — у секции две причины меняться. Этот таск владеет страницей, так что перенос здесь.
- [ ] **Порт e2e.** `playwright.config.ts` переиспользует 3100 и гоняет `next dev`, а поставляется статический экспорт `out/` — регрессия, ломающая только экспорт, остаётся зелёной.
- [ ] **Лепестки декора не ложатся светлым пятном на фотографию Laser Reveal** — видно только на собранной странице.

## Критерии приёмки

- [ ] `/[locale]/impressum` и `/[locale]/datenschutz` на трёх языках, ссылки в подвале, реквизиты из `studio.legal` заглушками, юридический текст помечен как «вписывает юрист»
- [ ] Метаданные на трёх языках: title, description, og-картинка, `hreflang` на все три локали, `canonical`
- [ ] Favicon-набор и монограмма из конфига
- [ ] Секции — `<section aria-labelledby>`, декоративные слои `aria-hidden`, фокус виден на пастельном фоне, порядок табов совпадает с визуальным
- [ ] `prefers-reduced-motion`: pin, вспышки, параллакс и курсор выключены, весь контент на месте — проверено e2e
- [ ] Вспышки: ≤3/сек, перепад относительной яркости < 0.1 — проверено тестом на вычисленной яркости, не на глаз
- [ ] Lighthouse desktop >95 по всем четырём категориям; mobile ≥90 — цифры приложить
- [ ] Нет горизонтального скролла на 360 px
- [ ] `content-visibility: auto` на тяжёлых слоях вне экрана; `fetchpriority` только у hero
- [ ] Playwright e2e: все секции достижимы; переключение языка сохраняет позицию; reduced-motion отдаёт контент; кнопка записи ведёт куда обещано; нет горизонтального скролла
- [ ] `npm test`, `npm run test:e2e`, `npm run lint`, `npm run build` — всё зелёное
